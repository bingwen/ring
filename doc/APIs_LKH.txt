API 需求:auth_token应该是每个用户唯一的用户标识吧，注册的时候产生，这样会比较方便做登录session这些。至于在哪里用你自己安排一下。user newUser POST params: email, gender, passwd return type: success T/F, auth_token “XXXXXX” , emailgetUser GET params: email，或者叫checkEmail。注册的时候检测邮箱是不是注册了updateUserPasswd PUT passwd auth_token, return success T/FupdateUserAvatar PUT  avatar(image data) auth_token, return success T/F path:””getUserInfo GET params:email return (user information)这个是在login里面直接写掉，返回json里面就包括user info还是单独写一个API你自己看。mood:newMood POST params: MoodId, Comments, 如果你想把文字和心情放在一起的话，不然就在下面单独弄个newComment 和getComments这样。return type: success T/FgetMoods GET return type: 这个你随意，看你database里面是哪些fields了，以及你返回的数据结构。然后我再看client怎么处理这些数据。design一下JSON的结构optional:newComment (这个API名字随意，天天说这文字是对心情的备注）POST return: success T/FgetComments GET returnlogin:1. sessions POST params: email, passwd return (user information)这样。